import { provideCloudflareLoader } from "@angular/common";

export const environment = {
    production: false,
    proxyUrl: 'http://localhost:3001'
};

export const employeeenvironment = {
    production: false,
    proxyUrl: 'http://localhost:3002'
}